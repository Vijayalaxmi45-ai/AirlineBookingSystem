# Example environment variables for backend (do NOT commit real secrets)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=airline_booking
DB_USER=root
DB_PASS=root

# Example options for production (use a managed DB and set strong password)
# PRODUCTION_DB_HOST=your-prod-host
# PRODUCTION_DB_USER=your-prod-user
# PRODUCTION_DB_PASS=your-prod-pass
